// import { useEffect } from 'react'
// import { useUser } from '../context/user'
const Canceled = () => {
  // const { Canceled } = useUser()
  // useEffect(Canceled)

  return <p>Payment Canceled. You have not been charged</p>
}

export default Canceled
